import { ExportExcelPipe } from './export-excel.pipe';

describe('ExportExcelPipe', () => {
  it('create an instance', () => {
    const pipe = new ExportExcelPipe();
    expect(pipe).toBeTruthy();
  });
});
